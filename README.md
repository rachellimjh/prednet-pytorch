# prednet-pytorch

This is a PyTorch implementation of [PredNet](https://github.com/coxlab/prednet). Including some experiments.

## Download the datasets

Run the following commands to download the required datasets:

```bash
bash download_data/mnist_download_data.sh # moving mnist dataset
bash download_data/kitti_download_data.sh # kitti dataset
```
