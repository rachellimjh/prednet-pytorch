DATA_DIR = "data/custom" 
# 1. ID (digits)
# 2. ID (vertical motion)
# 3. both?
NPZ_FILE = "ID_DISAPPEAR.npz"
KITTI_WEIGHTS= "models"
WEIGHTS_DIR = "models/mnist_id_digits"

RESULTS_SAVE_DIR="results/id_digits_disappear"
EVAL_ANOMALY_ONLY = True